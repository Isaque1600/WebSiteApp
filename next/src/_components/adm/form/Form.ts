import FormElement from "./FormField";
import FormRoot from "./FormRoot";
import FormSubmitBtn from "./FormSubmitBtn";

export const CustomForm = {
  Root: FormRoot,
  Field: FormElement,
  SubmitBtn: FormSubmitBtn,
};
